{
  "key": "PLANT-2",
  "components": [
    "utils",
    "middleware"
  ],
  "files_touched": [],
  "behaviors_asserted": [
    "auth(req) decodes the Authorization header and returns { username, password } in clear text",
    "the basic-auth middleware compares username and password itself using timingSafeEqual",
    "replace auth() with verifyBasicAuth(req, users) which decodes the header and does a constant-time comparison in one place, returning only true / false",
    "basicAuth() calls verifyBasicAuth(), and the { username, password } object is no longer produced anywhere",
    "remove auth() from src/utils/basic-auth.ts"
  ],
  "values_specified": {
    "authorization_header": "Authorization",
    "verify_basic_auth_return": "true / false"
  },
  "removes": [
    "auth"
  ],
  "depends_on": []
}
