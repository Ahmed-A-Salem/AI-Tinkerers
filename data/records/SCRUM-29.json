{
  "key": "SCRUM-29",
  "components": [
    "compose",
    "middleware",
    "types"
  ],
  "files_touched": [],
  "behaviors_asserted": [
    "compose intercepts errors at the inner dispatch and routes them to onError before the await unwinds",
    "when app.onError is registered, middleware that wraps await next() never sees errors thrown downstream",
    "next() with no argument behaves exactly as today",
    "when next({ rethrow: true }) is passed, the dispatch lets the error propagate out of await next() so the caller can try/catch it"
  ],
  "values_specified": {
    "next_options_type": "{ rethrow?: boolean }",
    "next_type": "(options?: NextOptions) => Promise<void>",
    "next_default": "no argument (current behavior)"
  },
  "removes": [
    "Next"
  ],
  "depends_on": []
}
