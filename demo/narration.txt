[1 · 0:00 · stand-up audio plays underneath]

A normal daily stand-up. Three engineers, one backend. Listen to Dev: he will build a new path helper on top of getPath from the URL utilities.

Nobody in that room knows a ticket already in the backlog removes getPath.

[2 · 0:30 · npm run meeting]

The first agent listens to the meeting, picks out the commitments, and turns each one into a draft ticket in Jira. Here is Dev's: title, description, acceptance criteria, label agent-draft. It can create drafts. It cannot do anything else.

[3 · 0:45 · npm run act -- SCRUM-205]

Creating the ticket wakes the second agent. It does not compare this ticket against all two hundred others. It pulls the few that share a component, a file, or an import edge in the code graph, and asks the model about only those.

It found one. The ticket goes red: label agent-conflict.

[4 · 1:00 · the comment]

Here is the comment. Both lines, quoted. Dev's ticket says build on getPath. The other ticket says remove the getPath export from src/utils/url.ts. And the code path that connects them: src/utils/url.ts, getPath. Not a guess from ticket text. The agent read the repository, and this file is what the router imports on every request.

[5 · 1:20 · add label agent-approved, then --apply]

This is Accept mode. The agent proposed, and it waits. A human adds one label, agent-approved. That is the whole approval interface. The agent links the two tickets. Nobody left Jira.

[6 · 1:35 · MODE=auto npm run act -- SCRUM-208]

Now Auto mode. A ticket that rips out the signed cookie helpers, against an open issue that needs them in the JWT middleware. Same pipeline, but it comments, labels, and links in one pass, nobody clicking. Safe, because those are its only three actions. There is no code path that edits a ticket.

[7 · 1:50 · backlog filter, npm run check -- --all]

Across the whole backlog: two hundred real Hono issues, six planted conflicts. Caught N of six, with M false positives. Two agents, living inside Jira.
